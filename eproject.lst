;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/Users/arch/.emacs.d/personal/preload/emacs-for-python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 23)
        (left . 4)
        (width . 80)
        (height . 35)))

